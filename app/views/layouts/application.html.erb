<!DOCTYPE html>
<html>
  <head>
    <title>AvtoTradingPlatform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%# <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
      <%# avto trading platform %>

  <body>
    <header>
      <span><%= link_to 'Recommendation', recommendation_index_path%></span>
      <span><%= link_to 'Recent', recent_path%></span>
      <span><%= link_to 'Dealers', dealers_path%></span>
      <span><%= link_to 'Add', add_index_path%></span>
      <% if user_signet_in? %>
        <%= current_user.first_name %>
        <span><%= link_to 'live', session_path, data: {mathod: :delete} %></span>
        <span><%= link_to 'save', edit_registration_path(current_user)%></span>
        
      <% else %>
        <span><%= link_to 'Sing In', registration_index_path%></span>
        <span><%= link_to 'log In', new_session_path%></span>
      <% end %>
    </header>

    <main>
      <%= yield %>
    </main>
    
  </body>
</html>
